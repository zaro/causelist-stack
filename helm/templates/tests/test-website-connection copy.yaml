apiVersion: v1
kind: Pod
metadata:
  name: "website-{{ include "causelist-stack.fullname" . }}-test-connection"
  labels:
    {{- include "causelist-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['website-{{ include "causelist-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
